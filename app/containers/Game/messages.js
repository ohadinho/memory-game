import { defineMessages } from 'react-intl';

export const scope = 'boilerplate.containers.Game';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'Welcome to an awesome memory game !',
  },
});
